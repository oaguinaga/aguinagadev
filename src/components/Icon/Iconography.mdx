import { Meta, IconGallery, IconItem } from "@storybook/addon-docs/blocks";
import Icon from "./Icon";
import { icons } from "./icons";

<Meta title="Design System/Iconography" />

# Iconography

This page showcases all available icons in the design system.

<IconGallery>
  {Object.keys(icons).map((iconId) => (
    <IconItem key={iconId} name={iconId}>
      <Icon id={iconId} size={24} strokeWidth={1} />
    </IconItem>
  ))}
</IconGallery>
